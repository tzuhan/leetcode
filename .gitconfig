[alias]
	co = checkout
	br = branch
	cm = commit
	st = status
	unstage = reset HEAD --
	last = log -1 HEAD
	loggraph = log --oneline --decorate --graph --all
	ad = add -i
	curbranch = rev-parse --abbrev-ref HEAD
[credential]
	helper = cache
[color]
	diff = always
