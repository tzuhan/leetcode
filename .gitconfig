[alias]
	co = checkout
	br = branch
	cm = commit
	st = status
	ad = add -i
	cmm = commit -m
	curbranch = rev-parse --abbrev-ref HEAD
	unstage = reset HEAD --
	last = log -1 HEAD
	loggraph = log --oneline --decorate --graph --all
[credential]
	helper = cache
[color]
	diff = always
